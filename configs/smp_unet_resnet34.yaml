# SMP U-Net with ResNet34 encoder
_base: _defaults.yaml

experiment_name: "smp_unet_resnet34"

model:
  architecture: "smp"
  smp_arch: "Unet"
  smp_encoder: "resnet34"
  num_classes: 2
  encoder_pretrained: true
