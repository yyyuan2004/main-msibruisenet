# TopFormer-Base (CVPR 2022) — ~5.1M params
_base: _defaults.yaml

experiment_name: "topformer_b"

model:
  architecture: "topformer"
  variant: "base"
  num_classes: 2
  pretrained_path: "pretrained/topformer-B-224-75.3.pth"
