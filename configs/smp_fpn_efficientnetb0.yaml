# SMP FPN with EfficientNet-B0 encoder
_base: _defaults.yaml

experiment_name: "smp_fpn_effb0"

model:
  architecture: "smp"
  smp_arch: "FPN"
  smp_encoder: "efficientnet-b0"
  num_classes: 2
  encoder_pretrained: true
