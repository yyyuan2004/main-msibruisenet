# PIDNet-Small (CVPR 2023) — ~7.6M params
_base: _defaults.yaml

experiment_name: "pidnet_s"

model:
  architecture: "pidnet"
  variant: "small"
  num_classes: 2
  pretrained_path: "pretrained/PIDNet_S_ImageNet.pth"
