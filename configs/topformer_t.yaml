# TopFormer-Tiny (CVPR 2022) — ~1.4M params
_base: _defaults.yaml

experiment_name: "topformer_t"

model:
  architecture: "topformer"
  variant: "tiny"
  num_classes: 2
  pretrained_path: "pretrained/topformer-T-224-66.2.pth"
