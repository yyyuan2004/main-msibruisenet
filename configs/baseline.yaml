# baseline — MobileNetV2-UNet (no enhancement modules)
_base: _defaults.yaml

experiment_name: "baseline"

model:
  num_classes: 2
  encoder_name: "mobilenetv2"
  encoder_pretrained: true
  skip_module: "none"
  use_spectral_conv: false
