# +SE: MobileNetV2-UNet with SE modules at skip connections
_base: _defaults.yaml

experiment_name: "se"

model:
  num_classes: 2
  encoder_name: "mobilenetv2"
  encoder_pretrained: true
  skip_module: "se"
  se_reduction: 16
  use_spectral_conv: false

data:
  num_channels: 9
  band_indices: null
