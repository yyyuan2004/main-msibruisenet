# PIDNet-Medium (CVPR 2023) — ~23M params
_base: _defaults.yaml

experiment_name: "pidnet_m"

model:
  architecture: "pidnet"
  variant: "medium"
  num_classes: 2
  pretrained_path: "pretrained/PIDNet_M_ImageNet.pth"
