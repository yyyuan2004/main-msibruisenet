# SMP MAnet with ResNet34 encoder
_base: _defaults.yaml

experiment_name: "smp_manet_resnet34"

model:
  architecture: "smp"
  smp_arch: "MAnet"
  smp_encoder: "resnet34"
  num_classes: 2
  encoder_pretrained: true
