# SMP UNet++ with ResNet34 encoder
_base: _defaults.yaml

experiment_name: "smp_unetplusplus_resnet34"

model:
  architecture: "smp"
  smp_arch: "UnetPlusPlus"
  smp_encoder: "resnet34"
  num_classes: 2
  encoder_pretrained: true
