# TopFormer-Small (CVPR 2022) — ~3.1M params
_base: _defaults.yaml

experiment_name: "topformer_s"

model:
  architecture: "topformer"
  variant: "small"
  num_classes: 2
  pretrained_path: "pretrained/topformer-S-224-72.3.pth"
