# SMP DeepLabV3+ with MobileNetV2 encoder
_base: _defaults.yaml

experiment_name: "smp_deeplabv3plus_mv2"

model:
  architecture: "smp"
  smp_arch: "DeepLabV3Plus"
  smp_encoder: "mobilenet_v2"
  num_classes: 2
  encoder_pretrained: true
